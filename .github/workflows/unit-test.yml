name: Run Unit Tests

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  contents: read
  pull-requests: read

jobs:
  run-unit-tests:
    uses: Card-360/common-github-actions/.github/workflows/worker-unit-test.yml@v0.1.0
