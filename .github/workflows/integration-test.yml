name: Run Integration Tests

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  contents: read
  pull-requests: read

jobs:
  run-integration-tests:
    uses: Card-360/common-github-actions/.github/workflows/worker-integration-test.yml@v0.1.0
